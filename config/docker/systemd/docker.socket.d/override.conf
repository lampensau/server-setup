[Socket]
# Secure Docker socket configuration
SocketMode=0660
SocketUser=root
SocketGroup=docker

# Security settings
NoDelay=true
KeepAlive=true

# CVE-2024-41110 protection
# Ensure only Unix socket is available, no TCP socket
ListenStream=
ListenStream=/var/run/docker.sock